services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: research-app-backend
    restart: always
    ports:
      - "4020:8000"
    volumes:
      - ./backend:/app
